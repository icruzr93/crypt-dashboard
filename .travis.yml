sudo: required
language: node_js
node_js:
- node

before_install:
  - chmod +x deploy.sh

script:
  - echo "Script"

before_deploy:
  - echo "BEfore deploy"

deploy:
  skip_cleanup: true
  provider: script
  script: bash ./deploy.sh
  on:
    branch: master